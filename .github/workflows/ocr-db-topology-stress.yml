name: OCR DB Topology Stress

on:
  push:
    branches: ["agent/**", main]
    paths:
      - "src/PhraseLayer.Core/PaddleDbPostprocess.cs"
      - "src/PhraseLayer.Core/PaddleDbQuadPostprocessor.cs"
      - "tools/run_ppocr_detector_db_differential.py"
      - "tools/run_ppocr_db_topology_stress.py"
      - "tools/requirements-ocr-db-audit.txt"
      - "experiments/PhraseLayer.DetectorOutputReplay/**"
      - ".github/workflows/ocr-db-topology-stress.yml"
  pull_request:
    paths:
      - "src/PhraseLayer.Core/PaddleDbPostprocess.cs"
      - "src/PhraseLayer.Core/PaddleDbQuadPostprocessor.cs"
      - "tools/run_ppocr_detector_db_differential.py"
      - "tools/run_ppocr_db_topology_stress.py"
      - "tools/requirements-ocr-db-audit.txt"
      - "experiments/PhraseLayer.DetectorOutputReplay/**"
      - ".github/workflows/ocr-db-topology-stress.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ocr-db-topology-stress-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  topology:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"
      - name: Install pinned host-only DB oracle tools
        run: python -m pip install -r tools/requirements-ocr-db-audit.txt
      - name: Stress holes and merged non-convex components
        run: |
          python tools/run_ppocr_db_topology_stress.py \
            --work-dir artifacts/ocr-db-topology/work \
            --report artifacts/ocr-db-topology/report.json
      - name: Upload numerical summary only
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ocr-db-topology-${{ github.run_id }}
          path: artifacts/ocr-db-topology/report.json
          if-no-files-found: ignore
          retention-days: 14
      - name: Remove local probability maps
        if: always()
        run: rm -rf artifacts/ocr-db-topology/work
